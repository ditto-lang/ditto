module Test exports (..);

import Data.Stuff (Five(..));

type Maybe(a) = Just(a) | Nothing;


with_default = (maybe: Maybe(a), default: a): a ->
    match maybe with
    | Just(a) -> a
    | Nothing -> default;

mk_five = (five) ->
    match five with
    | Five -> 5;
