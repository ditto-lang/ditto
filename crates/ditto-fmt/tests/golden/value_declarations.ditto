module Value.Declarations exports (..);


my_really_long_value_name: (oooooooooooooooooooooooooooooooooooooooong) -> Int =
    test();

inline_fives =
    -- fives
    [5, 5, 5, 5, 5];

hanging_fives = [
    --fives
    5,
    5,
];

foreign some_npm_function: (Int) -> Int;

update_fields = fn (r) ->
    -- updates a bunch of things
    {
        -- specifically...
        r |
            foo = 2,
            bar = { Stuff.r | baz = r.baz },
    };
